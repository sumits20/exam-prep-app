VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
VITE_API_URL=http://localhost:3001

# Not read anywhere in frontend/src today — Login.tsx calls
# supabase.auth.signInWithOAuth({ provider: 'google' }) with no client ID
# reference; Supabase Auth handles the whole Google OAuth flow. Only set
# this if a direct Google Identity Services integration is added later.
VITE_GOOGLE_CLIENT_ID=
