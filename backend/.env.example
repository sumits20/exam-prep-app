SUPABASE_URL=
SUPABASE_SECRET_KEY=
ANTHROPIC_API_KEY=
FRONTEND_URL=http://localhost:5173

# Not read anywhere in backend/src today — Google OAuth is handled entirely by
# Supabase Auth's dashboard-configured Google provider, not by this backend.
# Only set these if that changes.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
